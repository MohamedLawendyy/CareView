{
    "version": 2,
    "installCommand": "rm -rf node_modules package-lock.json && npm install",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}
